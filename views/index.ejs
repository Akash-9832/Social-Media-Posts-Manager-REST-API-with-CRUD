<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style.css">
    <title>All posts</title>
</head>
<body>
    <h1>Quora Posts</h1>
    <% for(post of posts){ %>
        <div class="post">
            <h3 class="user">@<%= post.username %></h3>
            <h4 class="content"><%= post.content %></h4>
            <a href="http://localhost:8080/posts/<%= post.id %>"><button class="details-info">See Details</button></a>
            <a href="http://localhost:8080/posts/<%= post.id %>/edit"><button class="edit-info">Edit post</button></a>
            <form method="post" action="/posts/<%= post.id %>?_method=DELETE">
                <button class="delete-info">Delete</button></a>
            </form>
        </div>
    <% } %>
    <a href="http://localhost:8080/posts/new"><button class="create-new-post">Create new post</button></a>
</body>
</html>

